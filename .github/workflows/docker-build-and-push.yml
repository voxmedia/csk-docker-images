name: Docker Build and Push

on:
  push:
    branches: [master]

jobs:
  middleman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build and Push
        env:
          IMAGE: middleman
        run: make build
